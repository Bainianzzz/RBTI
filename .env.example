# 外部 LLM 服务（服务端私有，server/api/llm.post.ts 通过 runtimeConfig 读取）
# 默认指向 DeepSeek（OpenAI 兼容）。换其它兼容服务只改这三项。

# API 基础地址（不含 /chat/completions，proxy 会自动拼）
API_URL=https://api.deepseek.com

# API key（在 https://platform.deepseek.com/ 申请）
API_KEY=

# 模型名
API_MODEL=deepseek-chat
